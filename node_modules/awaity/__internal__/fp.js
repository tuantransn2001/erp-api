'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.curry = curry;
exports.flip = flip;
function curryWrap(fn, len, prevArgs) {
  return function () {
    var newArgs = Array.from(arguments);
    var args = prevArgs.concat(newArgs);
    return args.length >= len ? fn.apply(null, args) : curryWrap(fn, len, args);
  };
}

function curry(fn, len) {
  if (len === undefined) {
    throw new Error('must specify args\'s length');
  }

  return curryWrap(fn, len, []);
}

function flip(fn) {
  return function () {
    var args = Array.from(arguments);
    var last = args.pop();
    return fn.apply(null, [].concat([last], args));
  };
}